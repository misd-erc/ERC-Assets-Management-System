# TODO: Implement Role-Based Access for Administration Modules

## Tasks
- [x] Import useAuthStore in AppSidebar.tsx
- [x] Move navigationGroups definition inside the component
- [x] Add logic to check if user has 'administrator' role
- [x] Conditionally include 'Administration' group in navigationGroups only for admins
- [x] Test the changes to ensure only admins see the Administration modules
